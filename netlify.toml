[build]
  command = "npm run build" # or yarn build, depending on your setup
  publish = "public"          # The directory where your Vite build outputs

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200